<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.acorn.MemberMapper">
 
    
 	<!-- 회원가입 -->
	 <insert id="regUser" parameterType="UserDTO">
        insert into user_table(id, password, name, contact_number, gender, birth_year)
        values(#{id}, #{password}, #{name}, #{contact_number}, #{gender}, #{birth_year})
    </insert>
	
	<select id="checkId" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user_table WHERE id = #{id}
    </select>
 
</mapper>